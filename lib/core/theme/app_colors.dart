import 'package:flutter/material.dart';

abstract class AppColors {
  static const background = Color(0xFFF2F3F5);
  static const primaryDark = Color(0xFF202020);
  static const accentLime = Color(0xFFC9F158);
  static const cardWhite = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF6B7280);
  static const textMuted = Color(0xFF9CA3AF);
  static const borderLight = Color(0xFFE5E7EB);

  static const statusPending = Color(0xFFFBBF24);
  static const statusSyncing = Color(0xFF60A5FA);
  static const statusSynced = Color(0xFF34D399);
  static const statusFailed = Color(0xFFF87171);
}
